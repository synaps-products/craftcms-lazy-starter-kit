<li class="ui-neo ui-lift bg-canvas flex gap-0">
  <div class="bg-pop border-r-2 border-ink flex items-center justify-center text-3xl w-16 shrink-0">
    {{ entry.emoji }}
  </div>
  <div class="p-4">
    <p class="ui-bold text-ink">{{ entry.headline }}</p>
    <p class="text-sm text-ink mt-0.5">{{ entry.text }}</p>
  </div>
</li>
